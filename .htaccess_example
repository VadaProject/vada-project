# Redirects all requests into the public/ directory.
# IMPORTANT: COPY this data into .htaccess.
# It can't be tracked, or it will clobber the .htaccess file generated by Hostinger. 
RewriteEngine on
RewriteCond %{REQUEST_URI} !public/
RewriteRule ^(.*)$ public/$1 [L]